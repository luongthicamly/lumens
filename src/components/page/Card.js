import React from 'react';
import BlockRental from '../includes/BlockRental';

function Card(props) {
    return (
        <div>
            <BlockRental/>
        </div>
    );
}

export default Card;